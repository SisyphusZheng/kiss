# My openElement App

Generated by [`@openelement/create`](https://www.npmjs.com/package/@openelement/create).
A file-based openElement app: pages live in `app/routes/`, shared UI in
`app/components/`, client islands in `app/islands/`, and blog posts in
`content/blog/`.

## Tasks

- `deno task dev` — start the Vite dev server.
- `deno task check` — type-check every route/component file. When you add a
  route file, add it to this task's file list in `deno.json`.
- `deno task test` — run Deno tests (the starter ships none).
- `deno task build` — production build (SSG + Nitro server) into `dist/`.
- `deno task start` — serve the production build. Use this for production
  preview: the starter ships a request-time route (`/contact`), so
  `deno task preview` (static-only preview, #601) is refused by design.
- `deno task preview` — static-only preview; refused for this starter (see
  above).

## Blog

`content/blog/*.md` posts are already wired to routes:
`app/routes/blog/index.tsx` (list) and `app/routes/blog/[slug].tsx` (post page,
prerendered via `getStaticPaths`). Copy `content/blog/welcome.md` to add a
post — `title` and `date` frontmatter are required. The routes import
`@openelement/generated/blog-data`, a module the adapter generates at
dev/build time; `app/data/_generated-blog-data.d.ts` provides its types for
`deno task check`.

## Import map notes

`deno.json` maps `@openelement/adapter-vite/nitro-mount` and `hono` even
though no source file imports them: the production build output (`dist/`)
imports them at runtime. Do not remove them.

## Brand assets

`public/openelement-mark.svg` is the openElement brand mark shipped with the
starter — use it for a favicon or page header, or delete it if unused.
