第七轮：清零所有剩余 P2/P3 项（+412/-167）

## P2 #17: 构建可观测性 (build-manifest.ts, +230 行)
- 新增 build-manifest.ts: 扫描构建输出、打印结构化摘要表
- Phase 2 (build-client) 输出: 每个 island chunk 大小、client entry 大小、总 JS
- Phase 3 (build-ssg) 输出: HTML 页面数/大小、headExtras 注入大小
- 预算告警: island > 50KB / 总 JS > 200KB / 页面 > 100KB
- 导出 BuildManifest/ArtifactInfo 类型 + scanClientBuild/scanSSGOutput 工具函数

## P3 #18: ssg-postprocess 健壮性重构 (ssg-postprocess.ts, 重写)
- 新增 insertAfterHead() / insertBeforeBodyClose() 正则辅助函数
- 处理边缘情况: <head> 带属性、</body> 有空白、大小写差异
- injectCspMeta: 检测已有 meta 标签(按 header name 而非完整 policy 匹配)
- CSP content 属性转义: 双引号 → &quot; 单引号 → &#39;
- injectClientScript: 用 insertBeforeBodyClose 替代裸 replace
- 不引入 parse5 — SSG 输出始终规整 HTML5，正则方案零依赖且足够健壮

## P3 #19: 设计 tokens 拆分模块 (tokens/, 4 个新文件)
- tokens/spacing.ts: spacing scale + border-radius + z-index + transitions
- tokens/typography.ts: font families/sizes/weights/line-height/letter-spacing
- tokens/colors.ts: dark/light theme color palettes (B&W Swiss style)
- tokens/effects.ts: box shadows
- design-tokens.ts 改为组合入口: import + css`${...}` 合并全部子模块
- index.ts 新增子模块独立导出 (支持 partial import)

## P3 #20: @hono/vite-dev-server 评估 (ADR #0001)
- docs/decisions/0001-keep-hono-vite-dev-server.md 完整 ADR 文档
- 结论: 保留。零生产足迹 vs ~100行替代代码的 DX 权衡
- README.md 加 ADR 引用链接
