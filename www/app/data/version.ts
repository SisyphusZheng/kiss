// Current source line. The npm registry line may lag by one alpha (see
// docs/status/STATUS.md); prose that claims a published line must not use
// this constant directly.
export const OPENELEMENT_VERSION = 'v0.42.0-alpha.17';

// Current npm registry line — the newest actually published five-package
// release. Published/stable copy must use this constant; OPENELEMENT_VERSION
// is reserved for source-line context.
export const PUBLISHED_PACKAGE_VERSION = 'v0.42.0-alpha.17';
