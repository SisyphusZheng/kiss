# v0.40.8 Cleanup-Train Audit Record

**Audit date**: 2026-06-19\
**Baseline**: v0.40.7 Release Readiness & CI Hardening\
**Target**: v0.40.8 Cleanup-Train Patch\
**Scope**: active source, tests, tools, current docs, and generated site surfaces

## Executive Summary

v0.40.8 is a cleanup-train patch that removes the legacy Linear UI
compatibility surface, migrates the last internal consumer to the canonical Open
Props UI, hardens the E2E server against local port conflicts, and extends the
audit-driven cleanup from explicit `any` to unnecessary `as unknown as` and
non-null assertions. No package count change, no default runtime or
signal-engine change, and no new product feature are introduced.

## Cleanup Checklist

| Item                                                                    | Status   |
| ----------------------------------------------------------------------- | -------- |
| Remove `open-*-linear` source files                                     | Done     |
| Remove `linear-token-sheet` source file                                 | Done     |
| Remove Linear exports from `packages/ui/src/index.ts`                   | Done     |
| Remove Linear subpath exports from `packages/ui/deno.json`              | Done     |
| Remove Linear manifest declarations/modules                             | Done     |
| Remove Linear smoke/component tests                                     | Done     |
| Update `packages/ui/README.md` (no Linear layer)                        | Done     |
| Migrate `www/app/islands/scroll-reveal.tsx` to `openPropsTokenSheet`    | Done     |
| Restore strategic anchors in `www/app/routes/guide/getting-started.tsx` | Done     |
| Add v0.40.8 version plan in `docs/current/VERSION_PLAN.md`              | Done     |
| Update `docs/roadmap/ROADMAP.md` and `docs/status/STATUS.md`            | Done     |
| Write `docs/release/v0.40.8.md`                                         | Done     |
| Bump workspace versions from `0.40.7` to `0.40.8`                       | Done     |
| Adjust E2E server strategy (`reuseExistingServer`, port fallback)       | Done     |
| Reduce `as unknown as` hotspots in production and tests                 | Done     |
| Split/reduce fake DOM/test helpers in `components.test.ts`              | Partial  |
| Extract repeated error formatting/runtime console fragments             | Deferred |

## Type-Safety Scan

- Explicit `any` in active TS/TSX: 0
- `as unknown as` in production: reduced; remaining entries reviewed in
  `tools/check-architecture-contract.ts` allowlist.
- `as unknown as` in tests: centralized in typed fake-event helpers in
  `packages/ui/__tests__/components.test.ts`.

## Remaining Debt

The following items are intentionally deferred to keep the v0.40.8 patch
bounded:

- Full extraction of `open-layout.tsx` navigation/theme/search helpers into
  separate internal modules.
- Full extraction of repeated error-formatting and generated-runtime-console
  patterns across all packages.
- Broader test-helper extraction beyond the `packages/ui` test harness.

These are tracked for future cleanup trains and do not block the v0.40.8 exit
gates.

## Verification

All static and build/test gates from the v0.40.8 Test Plan must pass before
merge. See `docs/release/v0.40.8.md` for the full acceptance matrix.

---

## 引用时效复核（自动生成）

> 本附录由 `tools/check-audit-citations.ts` 生成。基线：当前工作树。
> 引用总数：0；漂移：0。

全部引用均能在基线中解析，无行号漂移。
