# Bootstrap Prompt — v0.44 Internal Alpha Workspace Train

Use this prompt to launch the Alpha train. Do not invoke the three-role runner or a
release verifier.

Do not run `v044:executor:check` during Alpha. That preflight launches the dormant
Beta role profiles and is reserved for Beta.1 activation.

---

Complete OpenElement v0.44 Alpha as internal parallel workspaces.

Read the current Version Plan, Alpha workspace SOP, issue graph, execution plan and
state. Confirm #1193 safety, freeze the smallest shared code-first contract and real
path ownership, then record one exact common base SHA.

Create alpha.1 through alpha.7 as separate worktrees and branches from that SHA. Assign
one agent to each workspace. Each agent owns its implementation, focused RED/GREEN
tests, targeted gates, commits, branch and issue evidence end-to-end. Run them
concurrently. Never place two writers in one checkout.

Reserve alpha.8 for one final integration workspace and agent. It consumes the exact
accepted SHA from every other workspace, resolves integration failures, proves the
complete compiler/runtime/SSR/claim/app path, and opens the only Alpha full-CI PR to
`dev`. A failed integration produces another alpha.8 SHA.

Alpha identifiers are internal only. Do not tag, publish npm packages, create a GitHub
Release, change a dist-tag, promote `main`, request three-role GO, or invoke a fresh
release verifier. Stop after the exact green alpha.8 SHA is fast-forwarded to `dev` and
the Beta.1 handoff is ready.

---
