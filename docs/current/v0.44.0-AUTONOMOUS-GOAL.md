# Goal: close v0.44 Alpha.0 internally

Complete OpenElement v0.44 Alpha.0 as an unpublished foundation and
governance-convergence baseline on `dev`. The accepted starting baseline is PR
#1186 at `0ae65853a8e807b1b31da8752b434167219d25d6`.

## Scope

Alpha.0 contains #1156, #1160, #1182, #1187, #1188, #1189, and one exact-SHA
internal closure. #1160 and #1182 are accepted foundations. Do not implement
Alpha.1 or resume an Alpha.0-through-Beta.2 autonomous goal.

## Authority

The thinker owns reconciliation, packet selection, review, deterministic replay,
Git/GitHub operations, evidence judgment, and closure. The configured implementer
is the only role that changes production code, tooling implementation, workflows,
tests, fixtures, or executable control-plane configuration. A fresh configured
release verifier independently verifies one final candidate SHA and may only add
or strengthen tests and fixtures.

Invoke runnable roles only through `deno task v044:role`. Never substitute the
configured executor or reuse an implementer session as a verifier session.

## Closure

Freeze one candidate SHA after all Alpha.0 packets. Require authoritative exact-SHA
PR CI, implementer GO, a fresh release-verifier GO, and thinker GO. Integrate only
through a pull request to `dev`, close the Alpha.0 issues, save compact closure
references, and move the execution cursor to Alpha.1 planning.

Never tag Alpha.0, publish it to npm, create a GitHub Release, assign a dist-tag,
promote it to `main`, or begin Alpha.1 implementation.

Stop early only for a required architecture/public-API/security decision,
destructive-history decision, missing external authority after exact diagnostics,
unavailable configured executor without substitution, repeated proven SOP blocker,
or user interruption.
