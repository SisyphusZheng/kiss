# OpenElement v0.41.0-alpha.15 — Adoption Qualification and Stable Readiness

## Status

- Plan state: released and closed as an observation line; stable qualification blocked by #390 and #460
- Source and npm entry line: `0.41.0-alpha.15`
- Target line: `0.41.0-alpha.15`
- Stable candidate prepared by this work: `0.41.0`
- Stable release is not pre-authorized by this plan

## Outcome sought

Alpha.15 must answer one question with durable evidence: can the existing
five-package OpenElement product be frozen as `0.41.0`, or does real adoption
still require a breaking alpha correction?

The release combines four bounded outcomes:

1. supported GitHub Actions run on the current Node 24 action runtime;
2. current docs and version gates cannot drift behind registry truth;
3. external adopters complete the published path without private instructions;
4. the public five-package interface has a reviewable compatibility baseline.

## Work packages

### P1 — Governance and truth baseline

- [x] Merge this plan through `feature -> dev` before product implementation.
- [x] Set alpha.15 as the active execution target without changing alpha.14
      package manifests before release-prepare.
- [x] Correct README, roadmap, governance and current-contract maturity drift.
- [x] Add regression patterns to current-truth checks.
- [x] Record every later PR, gate and decision in this checklist.

### P2 — Actions runtime modernization

- [x] Supersede Dependabot #391, #392, #394 and #395 in one governed PR
      ([#455](https://github.com/open-element/openelement/pull/455),
      `417df64c`; AutoFlow, CodeQL, dependency review, Pages and advisory review
      passed).
- [x] Pin checkout 7, upload-artifact 7, setup-node 6 and dependency-review 5
      to reviewed immutable SHAs.
- [x] Update action version comments and mechanically validate comment/pin
      agreement.
- [x] Decide #393: OpenCode becomes advisory with Dependabot skip semantics, or
      the workflow is removed. Provider failures cannot block AutoFlow.
- [x] Prove nightly artifact upload/download (run `29486958896`: 30 minutes,
      359 requests, zero errors, both artifacts downloaded).
- [x] Prove release workflow dry-run with Node 24 actions and explicit Node 22
      npm execution.
- [x] Close #391–#395 as merged or superseded with evidence in their closing
      comments (the #393 unreviewed action bump remains intentionally unmerged).

### P3 — Cross-platform published consumers

- [x] Generate a fresh exact-version starter on Linux, macOS and Windows
      (qualification workflow added; it must run against alpha.15 after publish).
- [ ] Run starter check, test and build on every platform (the starter test task
      now explicitly permits an intentionally empty new-project test suite).
      Linux and macOS passed; Windows generation/check/test passed and build
      exposed #460 in run `29489167412`.
- [x] Verify Node ESM and Deno consumers for Element, App and Adapter Vite.
- [x] Verify Nitro Node and Workers through `nitro-mount`.
- [ ] Verify third-party Web Components and CDN consumption. The first run was
      invalidated by a missing Chromium install; #461 corrects the workflow and
      the rerun is required for closure.
- [x] Make failures retain structured environment and diagnostic output
      (`tools/published-consumer-qualification.ts`; reports are uploaded by the
      manually dispatched post-publish workflow).

### P4 — External adopter pilot #390

Deferred unchanged to the separate stable decision. No participant evidence is
claimed by alpha.15 and #390 remains open.

- [ ] Publish the pilot script, environment form, timing rubric, failure
      taxonomy, consent guidance and anonymized report template.
- [ ] Recruit three to five participants who are not project maintainers.
- [ ] Participants use only public npm artifacts and public documentation.
- [ ] At least one participant completes authoring, SSR or SSG, an island and
      deployment.
- [ ] Publish the anonymized result summary.
- [ ] File every P0/P1 finding; resolve all P0 and resolve or block on each P1.
- [ ] Close #390 only when the evidence is real and linked.

### P5 — Public interface freeze rehearsal

- [x] Generate deterministic export and declaration snapshots for all five
      packed packages.
- [x] Cover supported subpaths and reject accidental internal exports.
- [x] Freeze candidate behavior tests for `defineElement`, `definePage`,
      `defineApp` and `buildApp`.
- [x] Inventory defaults, error shapes, browser/runtime support and upgrade
      policy.
- [x] Decide the `@openelement/ui` stable-compatibility scope.
- [x] Add ADR and migration evidence for any pilot-driven breaking correction
      (not applicable: no pilot occurred and no pilot-driven correction was accepted).
- [x] Produce the `0.41.0` stable-readiness dossier.

### P6 — Alpha.15 release preparation

- [x] All alpha-release blockers in P1–P5 were complete before version bump;
      the real-user pilot remained a stable-only gate.
- [x] Run `autoflow:release-prepare --to 0.41.0-alpha.15` with this approved
      plan.
- [x] Verify five manifests, project constants, Create CLI and exact starter
      mappings are synchronized.
- [x] Update changelog, current docs, website and release note.
- [x] Pass a clean-tree repeat run without tracked-file mutation.

### P7 — Remote qualification and publication

- [x] Push `dev` and pass AutoFlow, CodeQL, dependency review and Pages.
- [x] Run Chromium, Firefox and WebKit functional E2E.
- [x] Pass package artifacts, pack dry-run and npm publish dry-run.
- [x] Merge `dev -> main` only after `dev` is green (#459).
- [x] Pass the same required checks on `main`.
- [x] Trigger the release workflow for `0.41.0-alpha.15` (run `29488948631`).
- [x] Verify five npm versions and five `alpha` dist-tags.
- [ ] Run fresh post-publish consumers on published artifacts.
      Linux/macOS and runtime boundaries passed as recorded above; Windows build
      remains a real published-artifact failure tracked by #460.
- [x] Create immutable tag, GitHub prerelease and final completed evidence.
- [x] Fast-forward `dev` to final `main`; verify identical SHAs before closure
      changes begin.

### P8 — Stable decision

- [ ] Compare pilot results and interface snapshots with the `0.41.0` gate.
- [ ] If no breaking work remains, publish a separate approved stable release
      plan; do not release stable from this alpha plan.
- [x] If breaking work remains, create narrowly scoped issues without weakening
      acceptance criteria (#460); the next alpha requires its own approved plan.
- [ ] Update #37 with the evidenced decision and keep it open until its actual
      stable gates pass.

## PR sequence

| Batch   | Scope                                         | Required base |
| ------- | --------------------------------------------- | ------------- |
| Plan    | P1 formal baseline                            | `dev`         |
| A       | P2 Actions runtime and OpenCode disposition   | `dev`         |
| B       | P3 pilot tooling and cross-platform consumers | `dev`         |
| C       | P4 pilot findings and fixes                   | `dev`         |
| D       | P5 interface freeze rehearsal and dossier     | `dev`         |
| Prepare | P6 version synchronization                    | `dev`         |
| Release | P7 `dev -> main`, publish and evidence        | `main`        |

Each feature PR identifies its completed checklist items, relevant ADRs, local
commands, remote checks and documentation impact. A failed external adopter
journey is a product result, not evidence to suppress.

## Test matrix

### Per PR

- `deno task fmt:check`
- `deno task lint`
- `deno task typecheck`
- targeted unit and fixture tests
- `deno task workflow:check` for workflow or governance changes
- `deno task docs:truth` for current-document changes

### Full qualification

- `deno task autoflow:ci`
- `deno task build`
- `deno task test:e2e:all`
- Nitro Node and Workers
- Linux, macOS and Windows published-consumer jobs
- local and packaged consumers
- core and third-party WC smoke
- package artifact and export/declaration snapshots
- npm pack and publish dry-runs
- clean-worktree repeatability

### Post-publish

- five exact npm versions and `alpha` dist-tags
- fresh exact-version Create starter
- Deno and Node ESM consumers
- Nitro Node/Workers
- third-party WC and CDN smoke
- immutable tag, GitHub prerelease and matching two-stage evidence

## Interface policy

No interface change is planned. Alpha.15 is the last intended observation line
before `0.41.0`; therefore a breaking correction is permitted only when backed
by pilot evidence, an ADR, migration guidance and compatibility tests. No new
package, JSR publication or speculative runtime seam is authorized.

## Release blockers

- Missing real participants blocks the separate stable-release decision, not this
  alpha observation release. #390 remains open as the durable adopter-pilot
  tracker; alpha.15 must not claim that the pilot occurred.
- Any unresolved P0 or unacknowledged P1 pilot finding.
- Current docs disagreeing with source, npm or active target.
- Action workflows depending on a blocking unreliable external review.
- Public export/declaration drift without an approved decision.
- Any failed required browser, runtime, packaged or post-publish consumer.
- npm, tag, release body or evidence disagreement.

## Completion definition

Alpha.15 is complete when the release evidence is complete and every deferred
stable-only requirement is linked to an open tracker. Publishing npm artifacts
alone is insufficient. Readiness for `0.41.0` is a separate decision and
requires a separate approved stable plan; #390 remains a mandatory input there.
