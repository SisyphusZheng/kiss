# v0.43.0-alpha.1 Release Plan

Status: APPROVED (2026-08-15, maintainer directive) · Line: 0.43 (Universal
WC SSR + diagnostics + recipes) · First train of the line: foundation +
governance debt, no big features.

## Scope

The 0.43.0-alpha.1 foundation train (from the 0.43.0-alpha.1 audit+train
session, prompt at the session root):

1. Full-project health check after the 0.42.0 freeze (freeze-surface guard,
   adversarial robustness, redundancy/bad-smell sweep, docs consistency) —
   findings fixed or filed (#972–#979).
2. Foundation items landed:
   - #631 hydration-mismatch structured diagnostics (dev detail / prod
     coded summary; OPEN_ELEMENT_HYDRATION_MISMATCH).
   - #969 cross-runtime dist/server: `package.json {type:module}` emitted,
     URLPattern floor guard on both serve.mjs and index.js (honest Node
     24+ floor with clean early errors), node-serve-smoke CI job.
   - #971 (alpha.2-scheduled, landed early): build-time hard failure when a
     definePage route's content element tag collides with the path-derived
     fallback tag; starter freshness template renamed off the collision.
   - Third-party WC SSR corpus foundation (third-party-wc:smoke task +
     machine-readable record).
   - Recipes verified against real artifacts: rate-limit (#627),
     FileDataAdapter (#629), better-auth guard (#630).
   - Governance: #965 (SSR registry markers chartered), #966 (host-API rule
     scoped), #967 (arch gate message), #623 (logger tags), #968 (dynamic
     route head.title), #970 (preact import-map recipe), #964 (CHANGELOG
     backfill), fetch-channel 422 symmetry fix.

## Entry conditions

- [x] 0.42.0 published and green (latest = 0.42.0)
- [x] All train items landed on main with repro-level tests
- [x] Full test suite + gates green on main (CI 0020892d)
- [x] No ADR-0119/0122 frozen semantics touched (freeze statement in the
      train report)

## Exit criteria

- npm `alpha` = 0.43.0-alpha.1 (five packages), tag + GitHub release
- Release note lists the foundation scope + the #971 early landing
- alpha.2 anchor issue (#979) recorded
