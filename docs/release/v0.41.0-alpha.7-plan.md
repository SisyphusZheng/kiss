# v0.41.0-alpha.7 Version Plan - Dogfood, Architecture Convergence, and Adoption Readiness

## Objective

Close the v0.41 alpha line with two connected outcomes:

1. Run a **read-only / accountless networked desktop dogfood app** against
   public Mastodon/GoToSocial-compatible APIs to stress-test the existing
   framework surface.
2. Convert the dogfood and architecture audit findings into an adoption-ready
   build path, public interface, security posture, evidence set, and release
   story before beta.1 verification.

Alpha.7 does not add server/data/forms/session/cache primitives. It deepens and
proves the existing v0.41 product surface.

The design source of truth lives in
`docs/dogfood/mastodon-desktop/README.md`.

## Product Intent

This is a **framework dogfood**, not a social client product. Reader (alpha.5/6)
proved local-first documents, annotations, and desktop reading ergonomics.
Mastodon Desktop proves a different surface:

- Remote public API fetching with realistic failure modes.
- Timeline-style navigation with dynamic content.
- Cache, error, retry, and rate-limit states as normal UI flows.
- Third-party Web Components used inside a desktop shell.
- Long-running SPA behavior under continuous data updates.

The app stays deliberately narrow so the framework learning stays sharp.

## Dependencies

Alpha.7 starts only after the following alpha.6 outcomes exist:

- OpenElement App owns RouteGraph, RenderPipeline, and RequestContext.
- Hono/Vite/Nitro are official adapters/drivers over OpenElement concepts.
- Deno Desktop is defined as a first-party OpenElement app target.
- Reader has a regression smoke matrix that proves the desktop app substrate.
- The first-party stack contract is documented.

## Workstreams

### A. Third-party Web Components Interop Foundation

- Close #221: restore third-party WC event interaction smoke without fixed
  sleeps.
- Expand smoke matrix to cover Shoelace forms/dialogs and Lit/openElement
  nesting.
- Ensure third-party WCs can be used inside the desktop shell before the shell
  depends on them.

### B. Desktop App Shell

- Create a macOS-focused Deno Desktop app scaffold in
  `examples/deno-desktop-mastodon/`.
- Reuse the alpha.6 Deno Desktop target contract instead of creating
  app-specific host glue.
- Provide sidebar, content panel, detail panel, settings, and status/error
  surfaces using OpenElement UI, Open Props, and optionally Shoelace.
- Keep the app keyboard-friendly and desktop-native in feel.

### C. Public Mastodon/GoToSocial Client Boundary

- Add a small public API client boundary for Mastodon/GoToSocial-compatible
  instance APIs.
- Support instance selection without login.
- Fetch public timeline, profile, and status/thread data.
- Surface rate limits, network failures, invalid instance URLs, and retry states
  as normal UI flows.
- Keep all API assumptions documented with fixtures or contract tests.

### D. Read-Only Social Surfaces

- Public timeline route.
- Profile route.
- Status detail/thread route.
- Local saved accounts/statuses as bookmark-like state.
- Basic search where public instance APIs support it.
- Empty/loading/error states for every route.

### E. Local State And Cache

- Store selected instance, recent profiles, saved statuses, and lightweight
  response cache locally.
- Keep credential storage completely out of scope.
- Add deterministic fixtures so tests do not depend on live network every run.
- Allow one live-network smoke to run manually or behind an explicit opt-in flag.

### F. Toolchain Truth

- Pin `.dvmrc` to Deno `2.9.0` (stable). The full gate matrix, including
  `deno pack` / `pack:dry-run`, passes on stable.
- Align CI: `setup-deno-workspace` already reads `.dvmrc`; update the standalone
  JSR consumer monitor to read `.dvmrc` too.
- Record the decision in `docs/current/VERSION_PLAN.md`,
  `docs/release/v0.41.0-beta.1-plan.md`, and `.dvmrc`.
- Keep Deno 2.8+ as the public minimum; canary remains acceptable only for
  Deno Desktop preview features outside the main gate matrix.

### G. Original Beta.1 Handoff (Completed And Superseded)

- The original beta.1 implementation issues were created and linked.
- The 2026-07-11 review moved that implementation scope into A7.01–A7.21 so
  beta.1 can remain release-candidate validation.

### H. Verification

- Browser smoke for route rendering and fixture-backed API flows.
- Deno Desktop smoke for shell boot, navigation, settings persistence, and
  native window behavior where supported.
- Contract tests for public API client parsing and error handling.
- Long-running stress test (30+ minutes) to check memory and stability.
- Screenshot smoke for the main desktop layout.

## Completed Dogfood Foundation

Parent PRD: #57.

| Slice | Issue | Area          | Status    | Completion proof                                                                  |
| ----- | ----- | ------------- | --------- | --------------------------------------------------------------------------------- |
| S0    | #221  | wc interop    | completed | third-party WC event interaction smoke is restored without fixed sleeps           |
| S1    | #195  | shell         | completed | macOS Deno Desktop app boots with OpenElement UI/Open Props shell                 |
| S2    | #196  | api client    | completed | fixture-backed Mastodon/GoToSocial public client parses timelines/profiles/status |
| S3    | #197  | timeline      | completed | instance selector and public timeline work without auth                           |
| S4    | #198  | profiles      | completed | profile route renders account metadata and recent statuses                        |
| S5    | #199  | status detail | completed | status/thread route supports navigation from timeline/profile                     |
| S6    | #200  | local state   | completed | selected instance, saved statuses, and lightweight cache persist locally          |
| S7    | #201  | verification  | completed | browser + Deno Desktop smoke proves the read-only workflow                        |
| S8    | #230  | toolchain     | completed | `.dvmrc` stable/canary strategy is decided and documented                         |
| S9    | #231  | handoff       | completed | the original beta.1 issue train was created                                       |

## Acceptance

- [x] #221 third-party WC event interaction smoke passes reliably.
      Verified locally: `deno task third-party-wc:smoke` scaffolds a fixture app
      that consumes lit / shoelace / @material/web and passes without fixed
      sleeps (S0 / #221). Wired as a CI/nightly gate.
- [x] The app boots as an OpenElement + Deno Desktop macOS dogfood app.
- [x] A user can choose a public Mastodon/GoToSocial-compatible instance without
      login.
- [x] Public timeline, profile, and status detail routes work from deterministic
      fixtures.
- [x] Public API failures and rate-limit states are visible and test-covered.
- [x] Local saved statuses/accounts persist without credentials.
- [x] Browser smoke and Deno Desktop smoke cover the core read-only workflow.
- [x] A 30+ minute stress run does not show obvious memory growth or UI freeze.
      #280 added a configurable Mastodon Desktop harness, RSS and latency
      thresholds, a nightly gate, and archived JSON evidence. The existing
      30-minute report records 359 requests, zero errors, RSS 39 -> 25 MB, and
      13 ms maximum scenario latency.
- [x] `.dvmrc` / Deno toolchain strategy is documented.
- [x] The original beta.1 implementation issues were created; their live work
      is now mapped into the alpha.7 A7.01–A7.21 train.
- [x] Docs clearly state that alpha.7 is a read-only/accountless framework
      dogfood, not a production social client.

## Pre-beta.1 release-readiness follow-up

The 2026-07-08 pre-release review identified release-pipeline blockers; #271
cleared them and is retained below as completed foundation evidence.

### Completed before beta.1 candidate validation

- [x] #271 B-1: `publish:npm` / `publish:npm:dry-run` run
      `generate:ui-manifest` first.
- [x] #271 H-1: internal dependencies pinned exactly (no `^` drift across
      alpha/beta lines).
- [x] #271 H-2: Cloudflare Pages deployment actually runs in release CI or is
      removed from the release gate list with a `STATUS.md` note.
- [x] #271 H-3: `publish:npm:dry-run` is either added to the `autoflow` release
      matrix or removed from `docs/status/STATUS.md`.

### Should fix during beta.1 window

- [x] #271 M-1 / M-2: `adapter-vite` public surface reconciled with
      `PACKAGE_SURFACE.md` and the document title updated to `v0.41.0-alpha.7`.
- [x] #271 M-3: provenance gated on `GITHUB_ACTIONS === 'true'`.
- [x] #271 M-4: `--allow-dirty` removed from real publishes.
- [x] #271 M-5 / M-6: `opencode-review.yml` and `autoflow-ci.yml` hardened
      (pinned action versions, permissions, concurrency, timeout).
- [x] #271 L-1–L-4: cleanup low-risk release tooling debt (leftover `.tgz`,
      router internal export, stale `bump-version.ts` header, alpha/beta npm tags).

## Completed Audit Foundation

The 2026-07-08 audit train (#275 through #285 and follow-ups) is complete. It
established the three-dimensional 80% package coverage gate, targeted runtime
tests, release evidence truth, repeatable stress automation, template
extraction, source-driven release anchors and package export synchronization,
and the first SSG module splits. Those closures are the baseline for the
current train; they are not open alpha.7 work.

## Alpha.7 Hardening And Adoption Task Train

The current train converts the dogfood evidence into a deep production build
module, a smaller authoring interface, explicit browser and security truth, and
an externally testable adoption path. Existing beta.1 and future architecture
issues were moved into alpha.7 so beta.1 can remain release-candidate
verification rather than an implementation train.

| Slice | Issue | Priority | Outcome                                                    | Blocked by                   |
| ----- | ----- | -------- | ---------------------------------------------------------- | ---------------------------- |
| A7.01 | #385  | P0       | release, roadmap, status, and documentation truth converge | none                         |
| A7.02 | #386  | P0       | browser baseline and native DSD contract are executable    | none                         |
| A7.03 | #382  | P0       | CI supply chain and dependency policy are auditable        | none                         |
| A7.04 | #383  | P1       | public security, support, conduct, and ownership exist     | none                         |
| A7.05 | #380  | P0       | production builds execute BuildPlan and return artifacts   | none                         |
| A7.06 | #381  | P0       | app/router owns SPA navigation, not OpenLayout             | none                         |
| A7.07 | #274  | P1       | UI has explicit token, primitive, and composite layers     | #381                         |
| A7.08 | #272  | P1       | adapter-vite exports match the author-facing interface     | #380                         |
| A7.09 | #273  | P1       | build, deploy, and resolver ownership is separated         | #380, #272                   |
| A7.10 | #387  | P1       | SignalEngine replacement claims match real implementations | none                         |
| A7.11 | #388  | P1       | critical runtime paths have risk-weighted coverage         | #380, #381, #274, #273       |
| A7.12 | #236  | P0       | an npm consumer completes the five-minute golden path      | #380, #272                   |
| A7.13 | #379  | P0       | npm metadata, provenance, and alpha channel are verified   | #382                         |
| A7.14 | #237  | P1       | public API and package surface documentation freezes       | #272, #387, #236             |
| A7.15 | #241  | P1       | Deno and Desktop support truth converges                   | #386, #236                   |
| A7.16 | #384  | P1       | competitor positioning is current and sourced              | #385, #237                   |
| A7.17 | #389  | P1       | dogfood performance evidence is reproducible               | #386, #380                   |
| A7.18 | #238  | P1       | the website presents one adoption-focused journey          | #236, #237, #384, #389       |
| A7.19 | #242  | P2       | brand assets render consistently across release surfaces   | #238                         |
| A7.20 | #390  | P1       | external users complete and report the adoption journey    | #383, #236, #238             |
| A7.21 | #240  | P0       | release evidence closes alpha.7 and hands off to stable    | all preceding alpha.7 slices |

Critical path:

```text
BuildPlan -> adapter surface -> five-minute starter -> public API truth
          -> adoption website -> external pilot -> release closure
```

Parallel tracks are architecture (#380/#272/#273), UI/browser
(#386/#381/#274/#388), trust/distribution (#382/#383/#379/#241), and
positioning/evidence (#385/#387/#237/#384/#389/#238/#242).

## Alpha.7 Exit Criteria

- [ ] All A7.01–A7.21 issues are closed with acceptance evidence.
- [ ] AutoFlow CI and the global line/branch/function 80% coverage gate pass.
- [ ] The browser baseline, BuildPlan, navigation ownership, adapter surface,
      and SignalEngine decisions match implementation and public documentation.
- [ ] A clean npm consumer completes the five-minute starter and Node/Workers
      deployment proof.
- [ ] Performance evidence is reproducible and the external adopter pilot has
      no unresolved P0/P1 finding.
- [ ] Source, npm, GitHub prerelease, website, and release evidence agree before
      beta.1 candidate validation begins.

## Non-Goals

- No OAuth/session flow.
- No direct messages.
- No notifications.
- No streaming API or background sync.
- No compose, reply, favorite, boost, follow, or authenticated mutation.
- No encrypted credential storage.
- No official mobile shell.
- No server/data/forms/session/cache framework primitives.
- No claim that OpenElement has complete server/data/forms/session/cache
  primitives because of this dogfood app.
