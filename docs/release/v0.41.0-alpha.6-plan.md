# v0.41.0-alpha.6 Version Plan - App Protocol Architecture Hardening

## Objective

Turn the alpha.5 Reader and architecture audit lessons into a cleaner
OpenElement framework architecture. Alpha.6 should make OpenElement's two core
products explicit:

- **Web Components fullstack framework**: app model, routing, rendering,
  islands, assets, deployment, and desktop targets.
- **Web Components Basic Element**: the small authoring layer behind
  `OpenElement`, SSR-safe lifecycle, render hooks, and custom-element ergonomics.

In public language, OpenElement is a Web Components-native fullstack framework
with a JSX-first Basic Element authoring layer.

Vite, Hono, Nitro, Deno Desktop, Preact islands, third-party Web Components, and
Open Props are official first-party stack choices. The alpha.6 work is to make
those choices enter through OpenElement-owned contracts instead of leaking as
package identity.

Status: released as `0.41.0-alpha.6`. The alpha.6 issue train is closed. The
front-half cleanup slice was extended with #226 and #227 to capture
duplicate-implementation and redundant-dependency debt discovered during final
review. All cleanup issues, the final CodeQL dynamic-import code-generation
alert, and the release-candidate cleanup PR (#229) were merged; the `0.41.0-alpha.6`
npm package workflow completed with provenance and post-publish consumer smoke
evidence.

## Product Stance

```text
OpenElement App owns: RouteGraph, RenderPipeline, RequestContext,
AssetManifest, IslandManifest, DeploymentTarget.

Official adapters implement: Vite build/dev, Hono request driver,
Nitro deploy output, Deno Desktop app target.

Reader proves: the framework survives a beautiful, stateful, local-first desktop
application.
```

OpenElement should not become "HonoX with Web Components." Hono remains the
default request/server driver, but `@openelement/app` owns the application
model. Vite remains the default build adapter, Nitro remains the default deploy
adapter, and Deno Desktop remains the preferred desktop dogfood target.

Dogfood apps validate OpenElement; they do not define OpenElement. Reader and
the alpha.7 Mastodon Desktop line may block release quality as evidence, but
they must not become product lines. AutoFlow3, docs truth, release evidence, and
workflow gates are infrastructure, not Framework features.

## Workstreams

### H. Front-Half Cleanup Audit Closure

- Treat #205 through #212, #226, and #227 as the first alpha.6 execution block
  before broad package/app/SSG reshaping.
- Close the concrete cleanup-audit findings: release truth drift, package
  surface drift, router internal export leakage, tracked ignored generated
  artifacts, stale resolver maps, repeated tooling helpers, duplicated
  route-scanner tests, stale design artifacts, active-source audit labels,
  duplicate implementations across packages, and redundant dependencies/configs
  in root `deno.json` and `tools/`.
- Keep the scope bounded. This block is repository hygiene and surface
  alignment, not a new architecture program.
- Use the results to make A6.1 through A6.7 safer: package boundaries,
  resolver behavior, and app/router exports should not preserve known residue.

### 0. Product Truth And ADR Closure

- Adopt ADR-0110 as the current public product doctrine:
  `Web Components Fullstack Framework + Basic Element`.
- Adopt ADR-0111 as the App ownership boundary before A6.2/A6.7 implementation.
- Sync README, STATUS, ROADMAP, VERSION_PLAN, PACKAGE_SURFACE, website-facing
  docs, and release plans so they agree on product formula, package graph, and
  active version line.
- Add a `docs:truth` gate so future releases catch product/version/package drift.

### A. Package And Protocol Separation

- Split package responsibilities so protocol-level contracts can be tested
  without booting the full default stack.
- Keep runtime-free/browser-facing packages free of ambient host assumptions.
- Keep Basic Element small and SSR-safe.
- Keep official adapters thin and explicit.

### B. App Model Ownership

- Define an OpenElement RouteGraph that is independent from Hono.
- Define an OpenElement RequestContext that is mapped from Hono in the Hono
  adapter.
- Define a RenderPipeline that covers page render, layouts, head/assets,
  islands, error handling, and output serialization.
- Ensure app model tests run without the Hono adapter, then add Hono adapter
  coverage separately.

### C. Official Stack Contracts

- Vite: official dev/build and asset manifest adapter.
- Hono: official request/server driver.
- Nitro: official deployment output adapter.
- Deno Desktop: official desktop app target.
- Open Props: token foundation for OpenElement UI and Reader.
- Preact: optional island authoring adapter, not the default OpenElement UI
  model.
- Third-party WC libraries: first-class compatibility target, not a replacement
  for Basic Element conveniences.

### D. Reader Regression Dogfood

- Promote Reader from "working proof" to regression-grade app.
- Keep Reader local-first and legally independent from WeRead.
- Use real public-domain/open fixtures where possible.
- Exercise source ingestion, PDF/text reading, search, annotations, note jumps,
  export, settings, Deno Desktop behavior, OpenElement UI, Preact islands, and
  third-party WC compatibility.

### E. Security And Static Analysis Closure

- Treat open CodeQL alerts as alpha.6 release hygiene, not a separate cleanup
  train.
- Close the currently open code-scanning backlog through focused, agent-ready
  issues: #186, #187, #188, #189, #190, and #191.
- Prefer structural fixes over suppressions: validated URL/specifier builders,
  parser-backed HTML/tag extraction, structured code-generation literals, and
  simpler async watcher state.
- Prioritize the real trust boundaries before broad package reshaping:
  - route codegen literals use a shared structured emitter instead of ad hoc
    string escaping;
  - island manifest extraction uses structured metadata or parser-backed HTML
    handling instead of regex stripping;
  - JSR source URL construction has explicit version/path/subpath allowlists;
  - dynamic import specifier admission is allowlist-first and rejects remote or
    ambiguous specifiers by default.
- Keep these fixes inside A6.6 unless a change exposes a deeper app/protocol
  contract issue that belongs in A6.1 through A6.5.

### F. Toolchain Truth

- Keep Deno 2.8+ as the public target.
- If `.dvmrc` remains `canary` to unblock release automation, document it as a
  temporary CI/release exception.
- Before beta.1, either restore a pinned stable Deno primary or explicitly make
  canary a documented release requirement with a non-blocking stable observation
  path.

## Alpha.6 Task Index

| Task   | Issue                                                                       | Area         | Blocked by                   | Completion proof                                                                                                                  |
| ------ | --------------------------------------------------------------------------- | ------------ | ---------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| A6.0   | #192 - Adopt two-product doctrine and sync product truth                    | docs/adr     | none                         | ADR-0110 is accepted and public docs agree on product formula, package graph, and active version                                  |
| A6.0b  | #193 - Record App ownership boundary in ADR-0111                            | adr/app      | none                         | ADR-0111 is accepted and A6.2/A6.7 can cite it instead of restating ownership boundaries                                          |
| A6.H1  | #205 - Align AutoFlow release with npm-only truth                           | release      | none                         | approved release plans pack/publish npm artifacts without required JSR publish closure                                            |
| A6.H2  | #206 - Align package surface docs, exports, and checks                      | package      | none                         | package exports and PACKAGE_SURFACE have one checked alpha truth                                                                  |
| A6.H3  | #207 - Hide router data-context internals from public root                  | router       | A6.H2                        | router root no longer advertises unstable `__internal_*` helpers as public API                                                    |
| A6.H4  | #208 - Normalize generated artifact tracking policy                         | repo         | none                         | tracked ignored generated files are deleted, moved, or explicitly documented and checked                                          |
| A6.H5  | #209 - Prune stale JSR resolver maps and consolidate export truth           | resolver     | A6.H2                        | resolver compatibility maps match active npm-first package exports                                                                |
| A6.H6  | #210 - Consolidate repo tooling helpers and remove dead package-utils       | tools        | none                         | dead helpers are removed and exact duplicate release/tool helpers are consolidated or classified                                  |
| A6.H7  | #211 - Move route-scanner ownership tests to SSG                            | test/ssg     | none                         | scanner semantics live in SSG tests while adapter-vite keeps glue coverage                                                        |
| A6.H8  | #212 - Clean stale design artifacts and active-source ponytail comments     | docs/repo    | none                         | stale mockups are refreshed/archived/deleted and active-source audit labels are normalized                                        |
| A6.H9  | #226 - Eliminate duplicate implementations across packages                  | core/app/ssg | none                         | tag validation, route parsing, redirect detection, error codes, and render helpers have a single source of truth                  |
| A6.H10 | #227 - Remove redundant dependencies, configs, and tooling helpers          | tools/repo   | none                         | root `deno.json` dependencies are trimmed, `tools/lib/` hosts shared helpers, and CI setup is deduplicated                        |
| A6.1   | #145 - Split `@openelement/content` into content-core and content-vite      | content      | A6.H2, A6.H5, A6.6 review    | content core is usable without importing Vite types                                                                               |
| A6.2   | #146 - Make SSG an OpenElement render engine with Hono/Vite default drivers | ssg/build    | A6.H2, A6.H7                 | route graph, render pipeline, asset manifest, and fs output are OpenElement concepts                                              |
| A6.3   | #147 - Extract Nitro as official deploy adapter and run real runtime proofs | deployment   | none                         | Nitro is no longer an adapter-vite side export; Node and Workers proofs execute generated output                                  |
| A6.4   | #148 - Support Basic Element and third-party WC page contracts              | app/interop  | none                         | manifest generation supports OpenElement and non-OpenElement custom elements                                                      |
| A6.5   | #149 - Decide and complete the `@openelement/core/hydrate` public contract  | hydration    | A6.4                         | hydrate/dispose semantics are documented, tested, and stable enough for Fresh/Preact/Reader usage                                 |
| A6.6   | #150 - Reduce governance/test fragility after alpha.5 ships                 | governance   | alpha.5 release              | brittle waits, CodeQL/trust-boundary alerts, oversized files, and scattered check scripts have focused follow-up or consolidation |
| A6.7   | #151 - Make App own RouteGraph, RenderPipeline, and RequestContext          | app/router   | A6.H3, A6.2, A6.4            | app model tests run without Hono; Hono adapter maps into OpenElement concepts                                                     |
| A6.8   | #152 - Document the first-party stack contract                              | docs         | #142                         | docs name Vite/Hono/Nitro/Deno Desktop/Open Props/Preact/WC roles and package boundaries                                          |
| A6.9   | #153 - Define Deno Desktop as a first-party OpenElement app target          | desktop      | #139                         | desktop target contract covers native window lifecycle, local source access, and smoke verification                               |
| A6.10  | #154 - Promote Reader into the regression-grade OpenElement dogfood app     | reader/test  | #137, #138, #139, A6.7, A6.9 | Reader smoke matrix fails when core framework workflows regress                                                                   |
| A6.6a  | #194 - Add `docs:truth` gate for product and version drift                  | validation   | A6.0                         | docs truth drift is checked by an explicit task and can fail on representative mismatches                                         |
| A6.6b  | Deno stable/canary toolchain truth                                          | tooling      | alpha.5 release              | Deno 2.8+ public target and any `.dvmrc` canary exception are documented with beta.1 convergence criteria                         |

A6.H1 through A6.H10 are the alpha.6 front-half cleanup-audit slice. They are not
a new architecture program; they close observed release, package-surface,
generated-artifact, resolver, tooling, test-ownership, docs, duplicate-implementation,
and redundant-dependency residue so the architecture work does not preserve stale truth.

A6.1 intentionally waits for the A6.H2/A6.H5 surface and resolver audit plus
the A6.6 trust-boundary review so the content-core split does not preserve known
CodeQL, resolver, or package-boundary debt inside the new package structure.

## Execution Order

1. Complete alpha.5 closure and release-note truth first.
2. Land A6.0 and A6.0b before broad implementation so public product language
   and App ownership are settled in ADR-0110 and ADR-0111.
3. Land A6.H1 and A6.H4 before any release or artifact policy change. The
   release path and generated-file policy must be true before alpha.6 broadens.
4. Land A6.H2, A6.H3, and A6.H5 before A6.1/A6.2/A6.7 reshape package, resolver,
   or app/router boundaries.
5. Land A6.H6, A6.H7, and A6.H8 before beta-facing polish so tooling, test
   ownership, and stale docs/design residue do not hide behind later work.
6. Land A6.8 early so architecture work has one shared vocabulary and stack
   contract.
7. Land the A6.6 trust-boundary fixes early when CodeQL/code-scanning evidence
   is open. They should not wait for the broader content split if the safer
   structural fix can land independently.
8. Land A6.1, A6.2, and A6.3 as adapter/package separation work. These can run
   in parallel if they do not rewrite the same public exports.
9. Land A6.4 before widening Reader or manifest expectations around third-party
   Web Components.
10. Land A6.5 after A6.4 clarifies what hydration supports for Basic Element and
    non-OpenElement custom elements.
11. Land A6.7 once SSG/app/page contracts are explicit; this is the central
    architectural slice.
12. Land A6.9 after the alpha.5 Reader verification workflow exists.
13. Land A6.10 last as the end-to-end dogfood proof.
14. Land A6.6a/A6.6b opportunistically after alpha.5 release, using evidence from the
    completed alpha.5 and alpha.6 test runs. Include the CodeQL cleanup issues
    #186 through #191 in this slice so security/static-analysis noise is closed
    before alpha.6 release review.

## Gates

- `deno task fmt:check`
- `deno task lint`
- `deno task typecheck`
- `deno task test`
- `deno task graph:check`
- `deno task package-surface:check`
- `deno task repo:hygiene`
- `deno task arch:check`
- `deno task deno-api:check`
- `deno task type-safety:check`
- `deno task docs:check-public`
- `deno task docs:check-current`
- `deno task docs:check-strategy`
- `deno task docs:truth`
- `deno task build`
- `deno task test:e2e`
- `deno task third-party-wc:smoke`
- `deno task nitro:proof:node`
- `deno task nitro:proof:workers`
- `git ls-files -ci --exclude-standard` returns only intentional, documented
  tracked-ignored fixtures if any remain after A6.H4.
- GitHub CodeQL/code-scanning alerts for #186 through #191 are fixed or closed
  with explicit evidence.
- Reader browser smoke and Deno Desktop smoke, once stabilized by #139/#153.

## Acceptance

- [x] Public docs describe the two primary products: Web Components fullstack
      framework and Web Components Basic Element.
- [x] ADR-0110 and ADR-0111 are accepted and indexed.
- [x] README, STATUS, ROADMAP, VERSION_PLAN, PACKAGE_SURFACE, and release plans
      agree on product formula, active version line, and current package graph.
- [x] Vite, Hono, Nitro, Deno Desktop, Open Props, Preact islands, and
      third-party WC libraries have explicit first-party roles.
- [x] `@openelement/app` owns route graph, request context, and render pipeline
      concepts; Hono is an adapter/driver over those concepts.
- [x] Nitro is an explicit deploy adapter or subpath, not an accidental
      adapter-vite public surface leak.
- [x] SSG exposes OpenElement render-engine concepts before Hono/Vite-specific
      details.
- [x] Content core can exist without importing Vite types.
- [x] Manifest/hydration contracts distinguish Basic Element conveniences from
      generic third-party WC compatibility.
- [x] Deno Desktop is documented and tested as a first-party app target, not as
      a localhost browser preview.
- [x] Reader has a regression smoke matrix that exercises the real framework
      stack.
- [x] The alpha.6 CodeQL cleanup backlog is closed: #186, #187, #188, #189,
      #190, and #191.
- [x] The alpha.6 product-truth backlog is closed: #192, #193, and #194.
- [x] The alpha.6 front-half cleanup backlog is closed: #205, #206, #207, #208,
      #209, #210, #211, #212, #226, and #227.
- [x] AutoFlow release behavior matches npm-only release truth; remaining
      JSR-related scripts are observation-only or removed.
- [x] Package exports, package-surface docs, and resolver compatibility maps
      agree on the active alpha package truth.
- [x] Generated artifacts, stale design mockups, duplicated scanner tests, dead
      tooling helpers, duplicate implementations, redundant dependencies/configs,
      and active-source audit labels are either removed, moved, or intentionally
      documented.
- [x] Route codegen, island manifest extraction, JSR source URL construction,
      dynamic import specifier admission, and generated entry JavaScript literal
      escaping use structural boundaries or have stricter tests proving the
      existing boundary.
- [x] Dogfood and governance surfaces are documented as evidence/infrastructure,
      not as product lines.
- [x] Deno toolchain truth is explicit: stable primary or documented canary
      exception, with beta.1 convergence criteria.

## Non-Goals

- No Mastodon/GoToSocial app in alpha.6.
- Mac Mastodon Desktop incubation is reserved for alpha.7 after the App,
  protocol, and Deno Desktop target contracts are clean.
- No new server/data/forms/session/cache product surface unless required to
  express the app/protocol contracts above.
- No React/Vue/Svelte adapter expansion beyond the existing optional Preact
  island path.
- No generic desktop abstraction over Tauri/Electron/Capacitor.
- No replacement of Vite/Hono/Nitro as the official defaults.
- No private WeRead API integration or copyrighted book fixtures in Reader.
