# v0.43.0 Release Plan

Status: APPROVED (2026-08-20, maintainer directive) · Line: 0.43 (Universal
WC SSR + Supabase × Cloudflare delivery path) · Stable cut: promotes the
published 0.43.0-alpha.2 train to the stable line; no new scope.

## Scope

The stable promotion of the consolidated alpha.2 train (same tree, version
line `0.43.0`, npm `latest` dist-tag moves from `0.42.0`):

1. Universal WC SSR track: admission visibility (#979 — scan/classify/record
   every consumed foreign tag) and the 0.43 product track (#996/#1020 —
   FAST/Stencil corpus, deterministic admission paths, machine-readable
   conformance matrix, browser evidence), plus hydration-mismatch structured
   diagnostics (#631).
2. Fullstack delivery path (epic #981, composition only): the reference
   starter and its evidence — auth/session/admin RLS/rate-limit/production
   mail (#998), storage/Realtime/Queue-DLQ/Cron/quota (#1000), signed
   webhooks, one-time Stripe Checkout and Cloudflare production operations
   (#1001).
3. Production closure evidence (#1002): Gate 0 (#999/#990/#987/#988/#994),
   Tier 2/Tier 3 evidence automation (#997), the closure train
   (#1040–#1054), the deep-scan batches (#1022–#1039, #1055–#1069) and the
   evidence-train hardening (#1072–#1082).
4. Freeze governance: 0.43 stable scope freeze ADR-0135 (mirroring
   ADR-0122), with frozen-path amendments ADR-0131/0133 and the
   evidence-freshness amendment ADR-0134 carried into the frozen set.
5. Deferred with owner/reason/target: real scan-engine evidence → v0.44
   (#1070, ADR-0132; fail-closed by default). #980 and #1083, registered as
   deferrals during alpha.2, landed before this cut (#1084, #1085) and ship
   in it.

## Entry conditions

- [x] RC evaluation passed: #1002 Phase F claim gate verified item by item
      (acceptance record in ADR-0135)
- [x] Freeze-scope ADR-0135 merged on main
- [x] blocked-human gates cleared (2026-08-19): Stripe CI secrets + webhook,
      GitHub OAuth journey, production SMTP (Resend, auth.openelement.org)
- [x] Provider evidence current and green: Tier 2 run 32285716457, Tier 3
      run 32286072210; `deno task fullstack:evidence-freshness` passes under
      ADR-0134 (the 08-17/08-18 scheduled reds are superseded by the newer
      manual greens)
- [x] main autoflow-ci green at the decision commit (run 32333313692 on
      `eb4ddedd`; the ADR commit's own CI verified before publish)
- [x] 0.43.0-alpha.2 published and consumer-proven (npm `alpha` dist-tag,
      post-publish consumer + third-party WC smoke green, run 32309769215)

## Exit criteria

- npm `latest` = 0.43.0 (five packages), tag `v0.43.0` + GitHub release
  (stable, not prerelease)
- Release note `docs/release/v0.43.0.md`: curated sections first, AutoFlow3
  evidence block appended verbatim; GitHub release body matches the file
- Version anchors advance: package/source/npm/landed lines read `0.43.0`;
  maturity stage moves to the 0.43 line; next release line `0.44.0`
  (production runtime) opens with #1070
- #1002 closed with the closure conclusion comment
