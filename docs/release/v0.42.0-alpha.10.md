# v0.42.0-alpha.10

> Status: `in-flight` — source line complete, npm publish pending (Phase 5 of
> the alpha.10 completion plan). The AutoFlow3 evidence blocks below are
> filled in by the publish-existing run; see
> [`docs/release/v0.42.0-alpha.9.md`](./v0.42.0-alpha.9.md) for the completed
> shape.

- Previous package line: `0.42.0-alpha.9` (npm registry line)
- Released package line: `0.42.0-alpha.10`
- Policy version: `autoflow3-v0`
- Milestone: `v0.42.0-alpha.10` (#17) — **97/97 issues closed**, open count 0

## Scope

Cleanup and hardening train — no new product surface; the ADR-0119 freeze
surface and the ADR-0120 Application Loop scope are untouched. Three batches:

1. **Milestone seed batch (#646–#653, closed 2026-07-30)**: release evidence
   chain repair (`persistFailedReleaseEvidence` + version-hole guard), ISR
   symbols demoted to `@experimental` with the KV adapter documented
   (`docs/current/ISR_KV_ADAPTER.md`), CHANGELOG holes filled, nested-WC
   submit e2e, silent-catch logging.
2. **Round-4 audit remediation (#654–#752, closed 2026-08-01)**: the fourth
   consistency/reliability/redundancy audit
   ([`docs/audit/2026-08-01-round4-hygiene.md`](../audit/2026-08-01-round4-hygiene.md))
   plus the backlog trains filed from the 2026-08-01 same-day audits — P1
   correctness (release gates, runtime error channels, docs truth), P2
   (SPA redirect, dead codegen/protocol surface, start/fixture unification),
   P3/P4 (barrel collapse, dead exports, archive conventions).
3. **Post-train CI repair**: clean-tree build evidence
   (`writeBuildEvidence` creates `.openElement` on ENOENT) and stale
   member-lock references dropped.

## Highlights

- Two-stage publish now refuses to run without a durable, gated prepare
  record (#684) — the alpha.8 version-hole class is closed mechanically.
- SPA loader/action semantics align with the server chain: loader failures
  take the error channel (#676); `redirect()`/`notFound()` navigate (#731).
- ui: double-escape removed (#726), open-tabs accessibility rewrite (#666),
  instance-unique ARIA ids (#745).
- element internal barrels collapse to a single source (#739); dead
  `internal/content/mdx/`, protocol seam shells and the JSR channel are
  deleted (#694/#697/#746).
- Docs truth: dual source/registry lines enforced by the version-anchor and
  www-truth gates (#730); `docs/integrations/` gated against the package
  surface (#737).

## Breaking (unfrozen alpha surface)

- `@openelement/app`: `i18nStaticPaths`, `switchLocale`, `AppIslandOptions`
  and the `./model` `OpenElementRouteNode` re-export removed (#743).
- `@openelement/ui/open-props-tokens`: `OPEN_PROPS_TOKEN_CSS` no longer
  exported (#747); generated manifest corrects open-tabs slots to
  `['tab','panel']` (#744).
- `@openelement/element` gains `@experimental` exports: ISR cache types
  (#729), `hydrateOpenElement`/`disposeOpenElement` (#740).

## Verification (pre-publish)

- `deno task test`: 1071+ passed / 0 failed (unit, full workspace)
- Contract gates: `arch:check`, `graph:check`,
  `signals:check-protocol-boundary`, `deno-api:check`,
  `package-surface:check` (now machine-checks www apilist),
  `export-files:check`, `interface:snapshot` (re-baselined as
  `docs/release/public-interface-snapshot.json`)
- Docs gates: `docs:truth` (public/current/strategy/anchors/evidence/www
  truth), `www:check-artifact-truth`
- e2e: request-time fixture build + clean-tree build evidence; www
  page-structure (36) and i18n-locale (12) specs; visual baselines
  re-recorded after the guide-shell refactor
- Main CI: AutoFlow CI green on the final train commit (see closure)

## Evidence

_To be completed by the publish-existing run (AutoFlow3 two-stage evidence,
including the #684 prepare record)._

## Durable closure

_To be completed at tag time._
