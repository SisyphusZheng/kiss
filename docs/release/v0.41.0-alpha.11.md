# v0.41.0-alpha.11

> Status: release candidate; publication is not claimed until npm, consumers,
> tag, GitHub prerelease, and final AutoFlow evidence agree.

## Highlights

- Correct query decoding and the SPA page-host loader/action contract.
- Restore dialog sibling inert state and unify theme propagation.
- Make Element the type-only protocol authority for Adapter Vite.
- Delete verified dead DSD, CEM, route-scanner, and UI escape internals.
- Restore clean Nitro Workers proof and semantic visual smoke.
- Exclude adapter tests and fixtures from npm artifacts.
- Raise publishable runtime coverage to 69% lines, 81% branches, and 72%
  functions with regression tests on audited paths.
- Split release preparation from publication of an already-reviewed,
  main-CI-green commit.

## Issue train

This release resolves #397-#411. Issue #396 closes only after the registry,
`alpha` dist-tags, published consumers, immutable tag, GitHub prerelease,
source, website, and final evidence have been reconciled. External adoption
pilot #390 remains a stable-release prerequisite and is not an alpha.11
publication blocker.

## Required publication evidence

Before this note changes from candidate to released:

1. the full clean pre-release matrix passes on `dev`;
2. the alpha.11 version commit passes `dev` and `main` CI;
3. all five packages publish in dependency order and own the `alpha` dist-tag;
4. Deno, Node ESM, exact-version starter, Nitro Node/Workers, and third-party
   Web Component consumers pass;
5. only then are `v0.41.0-alpha.11` and the GitHub prerelease created.
