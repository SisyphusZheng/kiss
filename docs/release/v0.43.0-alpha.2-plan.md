# v0.43.0-alpha.2 Release Plan

Status: APPROVED (2026-08-20, maintainer directive) · Line: 0.43 (Universal
WC SSR + Supabase × Cloudflare delivery path) · Consolidated train: carries
the planned alpha.2–alpha.6 scope in one cut (the trains interleaved on main
and cannot be sliced honestly after the fact).

## Scope

1. Universal WC SSR track: admission visibility (#979 — scan/classify/record
   every consumed foreign tag; CEM machinery for npm: packages) and the 0.43
   product track (#996 — FAST/Stencil admission evidence, deterministic
   admission paths, machine-readable conformance matrix, browser evidence).
2. Fullstack delivery path (epic #981, composition only): the alpha.3 risk
   spike (cookie session write-back through the Nitro cloudflare_module
   bridge, CSRF `c.env` floor on Workers — proven by the deployed reference
   starter), the alpha.4 reference app (#983; #998 auth/session/admin
   RLS/rate-limit/production mail), the alpha.5 recipe + PR-safe gates
   (#982 recipe, #984 tier 1; #1000 storage/Realtime/Queue-DLQ/Cron/quota),
   and the alpha.6 matrix + positioning (#985; #1001 signed webhooks,
   one-time Stripe Checkout, Cloudflare production operations).
3. Production closure evidence (#1002): Gate 0 (#999/#990/#987/#988/#994),
   Tier 2/Tier 3 evidence automation (#997), the closure train
   (#1040–#1054), and the deep-scan batches (#1022–#1039, #1055–#1069).
4. Evidence-train hardening (2026-08-19): browser-journey assertion shape,
   queue enumeration via the Cloudflare API, realtime re-seed on a bound,
   storage CDN delete-invalidation tolerance, runner apt-mirror resilience
   (#1072–#1079).
5. Pre-release hygiene sweep (#1076): 12 findings fixed, including two
   client-router guard-concurrency defects on the frozen path (regression
   tests + ADR-0133) and the open-dropdown SSR/CSR anchor sync.
6. Deferred with owner/reason/target: real scan-engine evidence → v0.44
   (#1070, ADR-0132; fail-closed by default); examples polish #980 → v0.44.

## Entry conditions

- [x] All train anchors closed: #979/#981/#982/#983/#984/#985/#996 and the
      #1002 children (#998/#1000/#1001/#997, Gate 0)
- [x] blocked-human gates cleared: Stripe CI secrets + webhook endpoint,
      GitHub OAuth journey (real login, server-side identity verified),
      production SMTP (Resend, auth.openelement.org; signup confirmation
      E2E on two mailboxes), scan engine deferred (ADR-0132)
- [x] Current-SHA provider evidence green: Tier 2 run 32285716457, Tier 3
      run 32286072210
- [x] Full gate suite green on main: 1607 tests, docs truth (claims +
      recipe parity), architecture, freeze semantics, package surface
- [x] Frozen-path touches carry amendment ADRs (0133; freshness-gate
      semantics amendment 0134)

## Exit criteria

- npm `alpha` = 0.43.0-alpha.2 (five packages), tag + GitHub prerelease
- Release note lists the consolidated scope and the deferral (#1070)
- Version anchors advance (package line 0.43.0-alpha.2; next planned train
  per the #1002 Phase F RC evaluation)
- The weekly scheduled Tier 2/Tier 3 runs continue green afterward (the
  freshness gate keeps its scheduled cadence for later versions)
