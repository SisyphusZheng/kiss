# v0.41.0-alpha.13 — #435 debt evidence ledger

Every low-severity audit item is resolved in alpha.13. “Complete” means the
implementation and its closest executable/documentary proof are present; final
closure additionally requires the integrated and post-publish gates in the
version plan.

## Element

| Item | Status   | Evidence                                                                                             |
| ---- | -------- | ---------------------------------------------------------------------------------------------------- |
| L1   | Complete | `internal/core/island.ts` imports the protocol error constant directly.                              |
| L2   | Complete | `render-dsd.ts` uses standard lookup/fallthrough terminology.                                        |
| L3   | Complete | `defineLayout` removed; ADR-0115, changelog, starter, README, and surface tests use `defineElement`. |
| L4   | Complete | `prop.ts` restores declared defaults; `open-element.test.ts` covers attribute removal.               |
| L5   | Complete | Signal-returning prop getters documented as intentional in Element README/authoring docs.            |
| L6   | Complete | Theme manager merges and deduplicates adopted sheets; theme tests preserve caller sheets.            |
| L7   | Complete | `EMPTY_RENDER` replaces the fallback double cast.                                                    |
| L8   | Complete | Render result uses property shorthand.                                                               |
| L9   | Complete | Default context signals use a `WeakMap<Context, Signal>`.                                            |
| L10  | Complete | Internal-only `resetWarnOnceForTests` and logger tests bound test state without public exposure.     |
| L11  | Complete | SSR params are rejected above 64 KiB with explicit tests.                                            |
| L12  | Complete | Nested SSR propagates depth and enforces `MAX_SSR_NESTING_DEPTH`.                                    |

## App and Adapter Vite

| Item | Status   | Evidence                                                                                         |
| ---- | -------- | ------------------------------------------------------------------------------------------------ |
| L13  | Complete | `SpaRequestCache` reuses only identical GET requests and rebuilds on URL/method/body changes.    |
| L14  | Complete | Client routes compile into a declaration-ordered static trie with equivalence/large-table tests. |
| L15  | Complete | Unused island chunk map removed from `ssg-render.ts`.                                            |
| L16  | Complete | Sitemap and SSG helpers share `internal/html-files.ts`.                                          |
| L17  | Complete | Build log names Phase 1 and build context explicitly.                                            |
| L18  | Complete | Missing optional i18n package emits an actionable warning.                                       |
| L19  | Complete | Default CORS emits config-entry/security warning; explicit configuration is silent.              |
| L20  | Complete | Public `__internal_*` exports removed; data-context store remains internal and surface-tested.   |
| L25  | Complete | Redundant SSG entry-code intermediate removed.                                                   |

## UI and Create

| Item | Status   | Evidence                                                                                                          |
| ---- | -------- | ----------------------------------------------------------------------------------------------------------------- |
| L21  | Complete | Tokens live in `open-props-tokens.css`; generated TS is checked by `generate:ui-tokens:check`.                    |
| L22  | Complete | Orphaned removed-option JSDoc deleted from blog option types.                                                     |
| L23  | Complete | `template-builder.ts` states and enforces the five-package same-version invariant; manifest test covers all five. |
| L24  | Complete | Template reads use `Promise.all`; target paths are sorted before output.                                          |

## Tools

| Item | Status   | Evidence                                                                                     |
| ---- | -------- | -------------------------------------------------------------------------------------------- |
| L26  | Complete | Architecture-check comments name the real Element protocol path.                             |
| L27  | Complete | Type-safety self-exclusions use `EXCLUDED_FILES`, not string concatenation.                  |
| L28  | Complete | Architecture TODO self-exclusions are explicit, not concatenated text.                       |
| L29  | Complete | Package surface derives from project constants and package exports.                          |
| L30  | Complete | Removed package directories derive from canonical removed-package data.                      |
| L31  | Complete | UI manifest scanner comment correctly describes lazy matching.                               |
| L32  | Complete | UI event extraction uses the TypeScript AST and supports nested detail.                      |
| L33  | Complete | Current-version matching derives from `PACKAGE_VERSION`.                                     |
| L34  | Complete | Text gate uses fatal UTF-8 decode plus replacement/mojibake detection.                       |
| L35  | Complete | Legacy HTML check only matches tagged templates.                                             |
| L36  | Complete | Consumer smoke defaults to `PACKAGE_VERSION`.                                                |
| L37  | Complete | Workflow ceiling is `MAX_WORKFLOWS`.                                                         |
| L38  | Complete | Missing deploy credentials fail in CI and remain an explicit local skip.                     |
| L39  | Complete | Unchanged release anchors emit a warning.                                                    |
| L40  | Complete | Architecture checker validates allowlisted files exist.                                      |
| L41  | Complete | Package imports are extracted with the TypeScript AST.                                       |
| L42  | Complete | UI subpaths derive from UI package exports.                                                  |
| L43  | Complete | Slow types fixed; ordinary typecheck and publish dry-run no longer use `--allow-slow-types`. |
