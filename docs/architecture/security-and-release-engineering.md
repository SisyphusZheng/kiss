# Security and release engineering

Pull-request CI is the complete authoritative matrix for an exact SHA. Release-only
verification consumes matching CI evidence and adds missing adversarial, artifact,
runtime, documentation, security, provenance, and exit checks. Machine-readable
unanimous role authorization, protected branches, Trusted Publishing/OIDC, Releases,
attestations, and npm provenance establish release trust.
