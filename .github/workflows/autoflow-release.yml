name: AutoFlow Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Target version (e.g., 0.41.0-alpha.1)'
        required: true
      plan:
        description: 'Release plan type'
        required: true
        default: 'patch'
        type: choice
        options:
          - patch
          - minor
          - major
      approvedPlan:
        description: 'Approved plan id (required for patch cleanup train or minor/major releases)'
        required: false

concurrency:
  group: openelement-release
  cancel-in-progress: false

jobs:
  release:
    name: Execute release
    runs-on: ubuntu-latest
    timeout-minutes: 120
    permissions:
      contents: write
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: ./.github/actions/setup-deno-workspace
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'
      - name: Install Playwright browsers
        run: deno run -A npm:playwright@1.59.1 install --with-deps chromium
      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
      - name: Resolve default approved plan
        id: plan
        run: echo "value=${{ inputs.approvedPlan || 'ADR-0105/v0.40.x-cleanup-train' }}" >> "$GITHUB_OUTPUT"
      - name: Configure npm auth
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > "$HOME/.npmrc"
          echo "registry=https://registry.npmjs.org/" >> "$HOME/.npmrc"
      - name: Patch release
        if: ${{ inputs.plan == 'patch' }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          deno task autoflow:patch-release --approved-plan ${{ steps.plan.outputs.value }}
      - name: Approved release
        if: ${{ inputs.plan != 'patch' }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          deno task autoflow:release --approved-plan ${{ steps.plan.outputs.value }} --to ${{ inputs.version }}
