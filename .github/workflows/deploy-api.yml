name: Deploy API to Deno Deploy

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'demo/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: denoland/setup-deno@v2
        with:
          deno-version: '2'
      - name: Deploy to Deno Deploy
        run: |
          # Copy to a clean dir (no workspace config)
          cp -r demo /tmp/kiss-api
          cd /tmp/kiss-api
          deno deploy \
            --app=kiss-demo-api \
            --prod \
            --token="${{ secrets.DENO_DEPLOY_TOKEN }}" \
            .
